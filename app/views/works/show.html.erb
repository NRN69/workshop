<% provide :page_title, 'Work' %>
<% currently_at 'Work'%>
<main class="my-5">
  <div class="container">
    <div class="text-center">
      <h1 class="mb-5"><strong><%= @work.title %></strong></h1>
      <div class="row">
        <div class="col-lg-4 col-md-6 mb-4">
          <div class="card">
            <div class="bg-image hover-overlay ripple" data-mdb-ripple-color="light">
              <div class="img-fluid">
                <%= image_tag(@work.img.url, size: '414x290') %>
              </div>
            </div>
          </div>
        </div>
        <div class="col-lg-8 col-md-6 mb-4">
          <div class="card">
            <div class="bg-image hover-overlay ripple" data-mdb-ripple-color="light">
            </div>
            <div class="card-body">
              <h4 class="card-title">
                <% @work.body.split(',').each do |line| %>
                  <ul>
                    <li><%= image_tag "check-circle.svg", size: '50', class: 'mx-3' %><%= line %></li>
                  </ul>
                <% end %>
              </h4>
              <p class="card-text">

                <%= render 'form', work: @work %>

              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>